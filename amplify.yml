version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'pip install -r requirements.txt'
    build:
      commands: []
  start:
    commands:
      - 'gunicorn --bind 0.0.0.0:8080 app:app'